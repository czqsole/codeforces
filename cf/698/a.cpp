//
// Created by 晁占强 on 2021/2/1.
//

#include <bits/stdc++.h>

using namespace std;
const int N = 100005;

int main() {
    int t, n;
    long long d[N];
    cin >> t;
    while(t--) {
        cin >> n;
        for (int i = 0; i < n; ++i) {
            cin >> d[i];
        }
        sort(d, d + n);
        int ans = 0;


    }
}
