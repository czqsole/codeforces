/*
 * @Author: czqsole
 * @Date: 2019-06-26 22:21
 */
#include <bits/stdc++.h>

using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    int q;
    cin >> q;
    while(q--) {
        int k, n, a, b;
        cin >> k >> n >> a >> b;
        int l = 0, r = n;
        while(l < r) {
            int mid = (l + r) / 2;

        }
//        cout << ans << endl;
    }
}